# Use Node
FROM node:18

# Set working dir
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy all files
COPY . .

# Expose port
EXPOSE 3000

# 🔥 IMPORTANT: bind to all interfaces
CMD ["npm", "start", "--", "--host", "0.0.0.0"]
